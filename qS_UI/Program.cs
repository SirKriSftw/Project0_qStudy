using System;

namespace qS_UI
{
    class Program
    {
        static void Main(string[] args)
        {
            MainMenu menu = new MainMenu();
            menu.displayTest(1);
            menu.displayTest(2);
        }
    }
}
